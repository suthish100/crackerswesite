import React from 'react';

export const metadata = {
  title: 'Shopping Basket | Sivakasi Crackers',
  description: 'Review your selected green crackers and festive combos before placing your order.',
};

export default function CartLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
