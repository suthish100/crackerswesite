import React from 'react';

export const metadata = {
  title: 'Track Your Order | Sivakasi Crackers',
  description: 'Check the real-time status of your Sivakasi fireworks order, verification progress, and transport dispatch.',
};

export default function TrackLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
